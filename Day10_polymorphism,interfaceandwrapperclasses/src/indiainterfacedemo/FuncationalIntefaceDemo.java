package indiainterfacedemo;
//Functional interface is a interface which contains exactly one abstract method
@FunctionalInterface
public interface FuncationalIntefaceDemo {
	//abstract method
		void print(String msg);
		
}
